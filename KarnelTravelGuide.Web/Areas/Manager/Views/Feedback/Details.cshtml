@model KarnelTravelGuide.Web.Models.Entities.Feedback
@{ ViewData["Title"] = "Feedback Details"; }

<div class="k-card p-4 bg-white rounded-4 shadow-sm border-0 mx-auto" style="max-width: 900px;">
    
    <div class="d-flex align-items-center border-bottom pb-3 mb-4">
        <a asp-action="Index" class="btn btn-light rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;"><i class="bi bi-arrow-left"></i></a>
        <h4 class="fw-bold m-0 text-dark">Feedback #@Model.FeedbackId</h4>
        <div class="ms-auto">
            <span class="badge bg-info text-dark fs-6 px-3 py-2 rounded-pill">Customer Review</span>
        </div>
    </div>

    <div class="row g-4 mb-5 border-bottom pb-4">
        <div class="col-md-6 border-end pe-md-4">
            <h6 class="fw-bold text-secondary text-uppercase mb-3"><i class="bi bi-person-circle me-2"></i>Customer Info</h6>
            <div class="mb-2"><span class="text-muted d-inline-block" style="width: 100px;">Name:</span> <strong class="text-dark">@Model.Account?.FullName</strong></div>
            <div><span class="text-muted d-inline-block" style="width: 100px;">Phone:</span> <strong class="text-primary">@Model.Account?.PhoneNumber</strong></div>
        </div>
        
        <div class="col-md-6 ps-md-4 mt-4 mt-md-0">
            <h6 class="fw-bold text-secondary text-uppercase mb-3"><i class="bi bi-box-seam me-2"></i>Service Info</h6>
            
            @if (Model.StayId != null) {
                <div class="mb-2"><span class="badge bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25 rounded-pill px-3 py-1">Stay</span></div>
                <strong class="text-dark fs-5">@Model.Stay?.Name</strong>
                <div class="small text-muted mt-1"><i class="bi bi-geo-alt-fill text-danger me-1"></i>@(Model.Stay?.Spot?.SpotName ?? "N/A")</div>
            } 
            else if (Model.RestaurantId != null) {
                <div class="mb-2"><span class="badge bg-success bg-opacity-10 text-success border border-success border-opacity-25 rounded-pill px-3 py-1">Restaurant</span></div>
                <strong class="text-dark fs-5">@Model.Restaurant?.RestaurantName</strong>
                <div class="small text-muted mt-1"><i class="bi bi-geo-alt-fill text-danger me-1"></i>@(Model.Restaurant?.Spot?.SpotName ?? "N/A")</div>
            } 
        </div>
    </div>

    <h5 class="fw-bold text-dark mb-3"><i class="bi bi-chat-right-text-fill text-primary me-2"></i>Feedback Content</h5>
    
    <div class="bg-light p-4 rounded-4 border mb-4">
        <p class="text-dark fs-5 fst-italic lh-base mb-0">
            "@(string.IsNullOrEmpty(Model.Message) ? "Customer did not leave a written message." : Model.Message)"
        </p>
        
        @if (!string.IsNullOrEmpty(Model.ReplyMessage)) {
            <div class="mt-4 pt-4 border-top">
                <span class="text-muted fw-bold d-block text-uppercase mb-2"><i class="bi bi-reply-all-fill text-success me-2"></i>Admin Reply</span>
                <p class="text-success fs-6 lh-base mb-0 p-3 bg-success bg-opacity-10 rounded-3 border border-success border-opacity-25">
                    @Model.ReplyMessage
                </p>
            </div>
        }
    </div>

    <div class="mt-4 pt-4 border-top text-end">
        <form asp-action="Delete" method="post" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this feedback? This action cannot be undone.');">
            <input type="hidden" name="id" value="@Model.FeedbackId" />
            <button type="submit" class="btn btn-danger px-4 py-2 fw-bold rounded-3"><i class="bi bi-trash me-2"></i> Delete Feedback</button>
        </form>
    </div>
</div>