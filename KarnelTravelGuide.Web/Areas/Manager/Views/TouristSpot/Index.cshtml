@model IEnumerable<KarnelTravelGuide.Web.Models.Entities.TouristSpot>
@{
    ViewData["Title"] = "Tourist Spots Management";
    int index = 1;
}

<div class="card shadow-sm border-0 rounded-4 mb-4">
    <div class="card-body p-4 d-flex justify-content-between align-items-center">
        <form asp-action="Index" method="get" class="d-flex w-50">
            <div class="input-group">
                <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" class="form-control bg-light border-0" placeholder="Search by name or address...">
                <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                <a asp-action="Index" class="btn btn-outline-secondary">Reset</a>
            </div>
        </form>
        <a asp-action="Create" class="btn btn-success fw-semibold"><i class="bi bi-plus-lg me-1"></i> Add New Spot</a>
    </div>
</div>

<div class="card shadow-sm border-0 rounded-4">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4">#</th>
                        <th>Image</th>
                        <th>Spot Name</th>
                        <th>Address</th>
                        <th class="text-end pe-4">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-bold text-muted">@(index++)</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.ImageUrl))
                                    {
                                        <img src="@item.ImageUrl" style="width: 70px; height: 50px; object-fit: cover; border-radius: 6px;" />
                                    }
                                </td>
                                <td class="fw-bold text-primary">@item.SpotName</td>
                                <td><i class="bi bi-geo-alt text-danger small"></i> @item.Address</td>
                                <td class="text-end pe-4">
                                    <a asp-action="Detail" asp-route-id="@item.SpotId" class="btn btn-sm btn-outline-info" title="View Detail"><i class="bi bi-eye"></i></a>
                                    <a asp-action="Edit" asp-route-id="@item.SpotId" class="btn btn-sm btn-outline-primary" title="Edit"><i class="bi bi-pencil-square"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.SpotId" class="btn btn-sm btn-outline-danger" title="Delete"><i class="bi bi-trash"></i></a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr><td colspan="5" class="text-center py-4 text-muted">No tourist spots found in your branch.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>